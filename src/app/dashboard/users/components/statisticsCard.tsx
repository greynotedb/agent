import { FC } from 'react'
import {
    Box,
    Card,
} from '@chakra-ui/react'

interface StatisticsCardProps {
  children: React.ReactNode
}

const statisticsCard: FC<StatisticsCardProps> = ({children}) => {
  return (
    <Card>
        
    </Card>
  )
}

export default statisticsCard